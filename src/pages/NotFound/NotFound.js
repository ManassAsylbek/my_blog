import React from 'react';
import style from "./NotFound.module.css"

const NotFound = () => {
    return (
       <h1 className={style.notFundTitle}>Страничка не найдена</h1>
    );
};

export default NotFound;